# Notion API 연동 설정
# 1. https://www.notion.so/my-integrations 에서 Internal Integration 생성 후 Secret 복사
# 2. Notion 데이터베이스 우측 상단 ⋯ → 연결(Connections) → 만든 integration 선택 (필수!)
# 3. 이 파일을 .env.local 로 복사한 뒤 실제 값을 채우고 개발 서버를 재시작

NOTION_TOKEN=
NOTION_DATABASE_ID=
